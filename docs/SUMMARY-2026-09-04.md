# สรุปงานประจำวัน — 2026-09-04

> เอกสารสรุปงานที่เพิ่มเข้ามาในวันที่ 2026-09-04 พร้อมผลการตรวจสอบ (type check + test pass)
> ดูรายละเอียดเวอร์ชันแต่ละขั้นใน `PROJECT_ROADMAP.md` → VERSION HISTORY (0.5 – 0.9)

---

## 1. สรุปภาพรวม

งานวันนี้เป็น **docs + staged design data** เป็นหลัก ไม่มีการเปลี่ยนกฎเกม (gameplay) ในตัว engine
ยกเว้นไฟล์ `state.ts` / `randomEvents.ts` / `randomEvents.test.ts` ที่เป็นงานค้างจากก่อนหน้านี้ใน working tree
(removed Random Event Effects — row 4a) และรวมอยู่ใน commit ชุดนี้ด้วย

ผลตรวจสอบยืนยันแล้ว:
- `pnpm check` (tsc --noEmit) ✅ ไม่มี error
- `pnpm test` ✅ **44 test files / 252 tests passed** + i18n check OK

---

## 2. เอกสารสเปกที่เพิ่มใหม่ (docs/proposals/)

| ไฟล์ | ระบบ / เนื้อหา |
|---|---|
| `negotiation-barter-play-ui-th.md` | Negotiation / Barter Conversation ในหน้า Play — negotiation panel, free-text offer, direct barter, quick actions, offer preview, NPC response states, dice check |
| `item-system-canonical-th.md` | Item System (Canonical) — category/kind/functions, immediate/reserve lifecycle, Classifier→Validator→Engine |
| `player-attachment-offering-th.md` | Player Attachment / Item Offering — ผู้เล่นแนบของ/เงิน/ข้อมูล/เจตนา แล้ว Engine validate |
| `time-system-core-th.md` | Time System — 3-layer (segment/day/season), dynamic activity duration, seasonal pressure |
| `sengoku-social-dynamics-th.md` | ชนชั้น/การตอบสนองตามสังคมเซงโกกุ — ตาราง class × เพศ × ฉาก |
| `rule-based-procedural-generation-th.md` | Content Contract + Validator สำหรับ Procedural Generation |
| `starter-quests-70-th.md` | Starter Quest Pack 70 เควส (10 อาชีพ × 7 ยุค) |

---

## 3. ไฟล์ดาต้าที่เพิ่มใหม่ (shared/data/)

| ไฟล์ | เนื้อหา | หมายเหตุ |
|---|---|---|
| `sengoku_economy_system.json` | Design ระบบเงิน/ราคา 3-tier (Copper/Silver/Gold), barter, warifu, regional price | เก็บเป็น design JSON คู่กับ roadmap row 37 |
| `sengoku_45_universal_events.json` | 45 events (ทุกฤดู/สถานที่) | **staged ยังไม่ merge** — เนื้อหาซ้ำ 44/45 กับ `random-events.json` แต่ Thai encoding ถูกต้อง |
| `sengoku_random_events_pool.json` | 8 events (ส่วนใหญ่เป็น id ใหม่) | **staged ยังไม่ merge** |

> หมายเหตุ: `sengoku_45_universal_events.json` คล้ายกับ `random-events.json` ที่มีอยู่ (44/45 id ซ้ำกัน)
> แต่เป็นเวอร์ชันที่แก้ encoding ภาษาไทยแล้ว — ยังต้องผ่าน merge/encoding fix + de-dup ก่อนนำมาใช้จริง
> (บันทึกไว้ใน roadmap row 4 แล้ว)

---

## 4. เอกสาร roadmap ที่อัปเดต (PROJECT_ROADMAP.md)

1. **Completeness Rule (กฎหลัก)** — เพิ่มใน Status Legend: ฟังก์ชัน [DONE] ต้อง (1) ครบวงจร
   (2) ไม่รันร่วมกับระบบที่ยังไม่เสร็จ (ระบบที่พึ่งพาต้องเสร็จก่อน) และ (3) มีหลักฐาน code+test ใน repo
2. **ตาราง Core Systems** — 41 rows (จากเดิม 38):
   - row 37 Economic & Currency System `[NOT FOUND]`
   - row 38 Negotiation / Barter Conversation `[NOT FOUND]`
   - row 39 Player Attachment / Item Offering `[NOT FOUND]`
   - row 40 Sengoku Social Dynamics `[IN PROGRESS]` (reuse sengokuSocialFacts)
   - row 41 Rule-based Procedural Generation Framework `[IN PROGRESS]` (reuse AI GM loop)
3. **คัดกรองระบบที่ซ้ำ (duplicate audit)** — ระบบที่ส่งเข้ามาแต่ซ้ำกับโค้ดเดิม ถูกผนวกกลับ
   เข้าระบบเดิม แทนที่จะเป็น row แยก: Item → row 9, Time → row 10/10a, Starter Quest → row 7/14, Random Post-Roll → row 4
4. **Not Implemented list / Next Steps** — ปรับให้ตรงกับหลังการคัดกรอง (งานส่วนใหญ่เป็น "ต่อยอดระบบเดิม")
5. **VERSION HISTORY** — เพิ่ม row 0.5 → 0.9

---

## 5. ไฟล์โค้ดที่รวมในข้อ push (งานค้างก่อนหน้า)

- `client/src/lib/game/state.ts` — grant/remove inventory effects (Random Event Effects, row 4a)
- `client/src/lib/randomEvents.ts` — ปรับปรุง random event effects
- `client/src/lib/randomEvents.test.ts` — test ครอบคลุม

> roadmaps row 4a ยังคง `[IN PROGRESS]` (semantics failure/negative ยังต้องตรวจเพิ่ม)
> — ยังไม่ถือว่าเสร็จสมบูรณ์ตาม Completeness Rule

---

## 6. สิ่งที่ยังไม่ได้ทำ / ต้องทำต่อ

- Merge + de-dup + แก้ encoding ของ `sengoku_45_universal_events.json` กับ `random-events.json` (row 4)
- Implement Event effect grant/remove ให้ครบตาม row 4a
- Implement ระบบ Economy/Negotiation/Attachment/Social/Procedural ตามสเปกที่เก็บไว้ (rows 37–41)